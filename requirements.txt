# VoiceGuard runtime + dev dependencies.
# Install torch/torchaudio from the CPU index for local dev:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt

# --- inference ---
torch>=2.4
torchaudio>=2.4
transformers>=4.44
huggingface_hub>=0.24
librosa>=0.10
soundfile>=0.12
numpy>=1.26
scipy>=1.11

# --- backend / api ---
fastapi>=0.114
uvicorn[standard]>=0.30
websockets>=13.0
httpx>=0.27
python-multipart>=0.0.9
pydantic>=2.7
pyyaml>=6.0

# --- dev ---
pytest>=8.0
